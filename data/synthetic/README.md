# Stage 3 synthetic data

Place phi-1-style synthetic data here: Docstring → Code → Tests, with optional CoT comments. Only include examples that pass unit tests (execution filter).

Format: JSONL with fields e.g. `problem`, `solution`, `test_code`, `tests_passed`.
